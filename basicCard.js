var fs = require("fs");

var BasicCard = function (front, back) {
    this.front = front;
    this.back = back; 
    }

BasicCard.prototype.logQuestion = function () {
    fs.
}

module.exports = BasicCard;